const configs = {
    "development": {
        host: "localhost",
        port: "3000",
        apiVersion: "v1",
        publicPath: "public",
        uploadPath: "uploads",
        mongoDburi: "mongodb://localhost:27017/messenger",
        secret: "secret"
    }
}

export default configs;